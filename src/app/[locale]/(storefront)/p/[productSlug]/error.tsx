"use client";

// See the same comment in `c/[...categorySlug]/error.tsx` — Next.js
// requires "use client" in this exact file, not just in the module it
// re-exports from.
export { StorefrontError as default } from "../../_components/storefront-error";
